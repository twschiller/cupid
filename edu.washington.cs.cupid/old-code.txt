		IWorkbenchWindow window = Workbench.getInstance().getActiveWorkbenchWindow();
		MenuManager menuManager = ((WorkbenchWindow)window).getMenuManager();
	
		ActionSetContributionItem menu = (ActionSetContributionItem)menuManager.find("cupidMenu");
		MenuManager cupidMenu = (MenuManager) menu.getInnerItem();
			
		cupidMenu.appendToGroup("cupidPipelines", new RunPipelineAction(pipeline, window));
		
		menuManager.update();	
		
		menu contribution
		 menu:org.eclipse.jdt.ui.PackageExplorer